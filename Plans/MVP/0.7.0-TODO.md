# TODO 0.7.0 — constitution

📌 **TL;DR:** Implement the constitution across storage, authority, lifecycle
and every face, in the shared 0.7 release line. The constitution owns the
model; this file owns the unfinished work and its acceptance. Rows marked
done link their evidence; every other row is pending.

## Objective

Deliver the [constitution](../../docs/constitution.md#project-constitution) in
the shared 0.7 release line. The constitution owns the model; this file owns
unfinished work and acceptance. K.1.2 has moved to R1. Existing behavior counts
as a starting point, not proof that the new contract is satisfied.

Keep the constitution's wording and structure. Update the numbered contracts
and user guides as each change lands. Existing [MVP acceptance](TODO.md#remaining-work)
remains open under its original IDs; completing this plan does not by itself
complete MVP.

## Starting point

Source review at 0.6.16: records and credentials use name references; Groups are
membership lists; records carry `disabled`; Service secrets accept nonempty
bytes. Administrative writes checkpoint before success but may mutate memory
before persistence. Five kinds, nested membership, Personal restrictions,
compacted JSON configuration and PubSub Deliver-To already have implementations.

## Next step

Start K.1, then SQLite and its exclusive lock in K.1.1/K.1.3 with the logs of
K.14/K.21/K.22, followed by K.2–K.6, K.19 and the identity and authority work; K.1.4 integrates clean reinstall after its
bootstrap and validation dependencies. The storage rows K.1–K.3 and the logs
K.14/K.21/K.22 are unblocked; [open questions](QUESTIONS.md#open-questions) name the rows each one
blocks. The
[forwarding contract](../../docs/constitution.md#-channels) is settled in full.
A reload API/SIGHUP remains optional and is not a required 0.7 feature.
Work lands on `main`. The 0.7 line has no other consumers yet, so the live node
may be rebuilt and restarted whenever the work needs it, and is reinstalled at
K.1.4.

## Storage and identity

An exhaustive deletion-specific crash matrix is excluded from 0.7. Agent and
record removal still use the commit-then-publish invariant and the focused
stale-grant checks in K.3/K.5. Users are never removed.

| ID | Work | Depends on | Acceptance |
|---|---|---|---|
| K.1 | Prepare the [clean 0.7 reinstall](0.7-cutover.md#scope) | — | **Done in 0.7.1** — the [reinstall procedure](0.7-cutover.md#procedure) specifies setup, Owner bootstrap, credential replacement and checks; the [performance baseline](0.7-baseline.md#results) is recorded |
| K.1.1 | Implement the default [SQLite store](../../docs/constitution.md#persistence-and-loading) behind persistence ports | K.1 | **Done in 0.7.1** — [evidence](done/sqlite-store.md#checks) |
| K.1.2 | Moved to [R1 storage](../R1/TODO.md#storage-acceptance) | — | Not a 0.7 prerequisite |
| K.1.3 | Take SQLite's exclusive lock before serving | K.1.1 | **Done in 0.7.1** — [evidence](done/sqlite-store.md#checks) |
| K.2 | Persist stable entity IDs, timestamps and derived indexes | K.1.3 | **Done in 0.7.3** — [evidence](done/entity-ids.md#checks) |
| K.3 | Apply management changes through write-through persistence | K.2 | Registration, profiles, Groups, management, credentials and private-value writes validate and commit in the selected backend before memory publication. Core publishes one complete replacement view from the validated committed change rather than mutating live maps in place or reloading storage; a publication failure refuses every read/write with a reason. Failed validation or pre-commit save exposes no partial update; test late storage failures and restart outcomes explicitly. Publishing before commit, retaining old authority after commit or allowing a stale queue batch to rewrite policy fails its check |
| K.4 | Enforce User ownership everywhere | K.2, K.3 | **Done in 0.7.5** — [evidence](done/actor-kinds.md#checks) |
| K.5 | Bind credentials to typed User/Agent IDs and invalidate stale grants | K.2–K.4, K.21 | **Done in 0.7.6** — [evidence](done/credential-pairs.md#checks) |
| K.6 | Complete User profile validation and lifecycle fields | K.2, K.3 | Check unique normalized email, GitHub and case-insensitive Twitter/X names on add, edit, import and restore; person names remain nonunique. Preserve enrolment keys and profile/photo data. Invalid multi-field edits are atomic. Bypassing any uniqueness check fails its negative case |
| K.19 | Make the realm optional in every [name](../../docs/01-identity-and-roles.md#names) | K.2 | Parsing accepts `name`, `name@realm` and `template/instance@realm`, splitting the template at the first `/` and the realm at the last `@`; a realm-less name carries no `@`. `alice` and `alice@srv1` are distinct principals that may both exist, each addressable only by its own spelling, and rendering a realm-less name appends no realm. A new User defaults to the Unix account name alone, and no face, launcher or setup path completes a bare name with the hostname. Every name accepted before cutover keeps its template and realm split; only an Agent's name gains the `#` of K.8. Setup bootstraps the daemon Owner under the bare account name, and launchers keep `#runtime/instance@host`. Treating a realm-less name as an alias of a realm-qualified one, appending a realm anywhere, or splitting on any `@` but the last fails its own check The [name syntax table](../../docs/01-identity-and-roles.md#names) stays the grammar for every kind: one daemon parser (`protocol.ParseName`) enforces it, now also after an Agent's `#` and a Group's `@`, and faces defer to it rather than keep their own rules; a leading digit stays valid, so `#1worker` is an Agent named `1worker` **Realm half built in 0.7.4** — `ParseName` takes `name` alone, renders no realm, and setup defaults the Owner to the bare account name; `TestARealmIsOptional` covers the split and the bound. The `#` and `@` prefixes follow in K.8 |
| K.1.4 | Implement [clean reinstall setup](0.7-cutover.md#procedure) | K.1, K.1.1, K.1.3, K.2–K.6, K.8, K.9, K.11, K.13, K.19, K.20 | Stop the old installation and initialize fresh SQLite state with matching binaries. Bootstrap Owner and local-account access; recreate resources through validated 0.7 paths and issue new credentials. Verify that old snapshots, credentials and backlog are not imported. Reinstall is explicit; startup failure never silently creates an empty replacement database. Removing isolation or accepting an old credential fails its check |

## Authority and lifecycle

| ID | Work | Depends on | Acceptance |
|---|---|---|---|
| K.7 | Authorize existing state; validate the whole candidate | K.3–K.5 | Old Owner can transfer; new Owner governs immediately and after restart. A submitted grant cannot authorize itself. Maintainers cannot alter protected fields; Agent self-management and Owner-only Personal changes hold. Concurrent revocation cannot race a stale authorization into a write. Each bypass has a failing check |
| K.8 | Add typed actor syntax while retaining existing forms | K.5, K.7 | **Done in 0.7.5** — [evidence](done/actor-kinds.md#checks); line-numbered editor errors are K.16 |
| K.20 | Scope [Personal](../../docs/03-records.md#personal-and-shared) to the Owner's own cohort | K.7, K.8 | **Done in 0.7.5** — [evidence](done/actor-kinds.md#checks) |
| K.9 | Give Groups User Owners and assigned Maintainers | K.4, K.7, K.8 | A Group is a registry record carrying `kind` = `group` from the same closed enum and a `registry_id` from the one shared ID space; a Group loaded or written without the kind is refused, and no Group ID collides with or duplicates another record's. Assigned Users/Agents manage ordinary Group membership; only permitted authority changes Maintainers. Protected `@administrators` follows the [protected Group rule](../../docs/constitution.md#-group): verify its Owner matches the daemon Owner on creation, restore and any daemon ownership change, reject an independent Group ownership change or supplied Maintainers, and let only the daemon Owner change its direct membership. Nested cycles terminate; revocation takes effect. Supplying either forbidden field or a Group Maintainer attempting to grant daemon administration fails acceptance |
| K.10 | Add atomic list add/remove operations | K.3, K.8, K.9 | ACL, Maintainers, Group members and Deliver-To support concurrent deltas without lost updates. `deliver_to` exists on PubSub, Agent and Queue only, and a User record carrying one is refused. A PubSub recipient list resolves each term against the registry and accepts an Agent, Group, Queue or PubSub; the one-slot form accepts an Agent, Queue or PubSub. A User, a Service and a name with no record are refused separately in both, for kind rather than for permission. A User's inbox takes a direct send from an Agent exactly when that Agent's ACL admits the User, and never from another User or from an Agent that does not admit the User; a published copy or route to a User is answered with an error rather than discarded or counted as a drop. A User-to-User send succeeding, an admitted Agent's reply refused, a non-admitting Agent's send accepted, or a copy to a User dropped silently fails separately. On Agent or Queue it accepts at most one destination: add succeeds only when empty; an occupied add returns an error naming the field, remove clears it, and replacement is an explicit whole-field write. A whole-field write with two destinations or a wrong-kind term is refused and stores nothing. An invalid mixed delta changes nothing; concurrent adds cannot silently overwrite or no-op |
| K.11 | Replace User states and record Disabled with active/inactive | K.4, K.5, K.7, K.14 | No banned state or second delivery switch remains. User inactivity suspends access and directly owned records without deleting tokens/backlog; record inactivity blocks reads/writes, releases affected waits, and makes the record no such entity. A caller is refused exactly when nothing gets through. A publication with one inactive recipient among active ones succeeds, discards that copy, increments that record's `dropped` and writes one daemon log entry naming topic and recipient; a publication whose recipients have all failed, a direct send to an inactive name, and a route whose one destination is inactive are each answered with an error and a log entry, storing and counting nothing. An inactive Group term is ignored with a log entry and adds no `dropped`, and a publication left with no recipient by that expansion is refused. Refusing a publication that still had a live recipient, succeeding when none did, counting a drop against a Group, dropping without a counter or a log line, and answering a broken flow with silence each fail separately. An active Administrator may reactivate an ordinary User but cannot change another Administrator; the daemon Owner may reactivate either, and must remain active. Removing the peer-Administrator refusal or bypassing authority, socket/session or pending-reader checks fails acceptance The `disabled` refusal reason disappears: an inactive target answers `unknown` (`404`) and an inactive caller `suspended` (`403`); answering either with the other, or keeping `disabled`, fails its check |
| K.12 | Hide every inactive record while reserving its name | K.7, K.11 | An inactive record of any kind, and every record of an inactive User, appears in no discovery, lookup, send, consume, drain or secret read and answers as unknown (`404`), never `disabled` or `suspended`; an inactive Group grants nothing, so a membership path through it reaches no actor; registration at the name fails. One read-only call for the web face shows inactive records to the actors their `allow` admits and always to the daemon Owner, and performs no other operation. Only the record's Owner or a Maintainer reactivates it, by a status edit without recreation. Removing a User's 👤 record is refused, and a User found without one at startup is a [conceptual error](../../docs/constitution.md#errors-and-alerts). Cover every face and restored state. Removing hiding or uniqueness enforcement fails separate checks |
| K.13 | Validate env secrets and JSON configuration on 👾, 📡 and 👥 | K.3, K.7, K.9 | `config` and `secret` are accepted on exactly the kinds the [field table](../../docs/constitution.md#common-record-fields) gives them and refused on every other; the record's own principal reads them, and on a 👥 every member does. Basic env syntax is enforced without application-specific validation; JSON is validated and compacted before hashing. Enforce kind and read/write authority, retain digest-only public responses and reject invalid updates atomically. A nonconforming secret is rejected on write and target-state validation; no legacy exception is loaded. Invalid env input, wrong-principal config reads and uncompact hashing each have a failing check |
| K.14 | Log administrative actions and entity edits to the [audit log](../../docs/constitution.md#logs), `audit.log` | K.1.1 | **Done in 0.7.2** — [evidence](done/daemon-logs.md#checks) |

## Delivery and release

| ID | Work | Depends on | Acceptance |
|---|---|---|---|
| K.21 | Report [conceptual errors and alerts](../../docs/constitution.md#errors-and-alerts) to syslog and the error log | K.1.1 | **Done in 0.7.6** — [evidence](done/credential-pairs.md#checks); `error.log`, syslog and logrotate since 0.7.2 ([evidence](done/daemon-logs.md#checks)) |
| K.22 | Write the on-demand [debug log](../../docs/constitution.md#logs) of every request | K.1.1 | **Done in 0.7.2** — [evidence](done/daemon-logs.md#checks) |
| K.15 | Forward Agent and Queue traffic | K.8–K.11, K.21 | Implement the zero-or-one destination meaning of `deliver_to` on Agent and Queue while retaining its recipient-list meaning on PubSub; a second destination is an error, never replacement, no-op or truncation. Every forwarding step, a topic's fan-out into a channel included, increments the envelope's forward counter; the step that would raise it above ten is refused with a stated error before storage, leaving every inbox unchanged. Two topics listing each other, and a queue routing back into the topic that feeds it, both terminate with that error rather than looping or dropping silently. Follow the [forwarding ACL contract](../../docs/constitution.md#-channels): test sender S allowed by Agent A, with B listing A but excluding S and A's Owner; configuration and delivery succeed. Refuse configuration or delivery when B does not allow A, even if B allows S or A's Owner; delivery refusal leaves the route configured. Reject S when A denies S. Cover Agent and Queue sources; caller-supplied provenance cannot grant forwarding authority. Apply B's TTL/deadline, bound, overflow and counter rules as for a direct send; an inactive or absent B refuses S, because the one slot leaves the message nowhere else to go. Any refusal answers the original sender before storage or counters change; a later route grant resumes forwarding without an edit. Move exactly one message to the destination with the original sender and required single-valued `original_to`: source `in`/`out` stay unchanged, destination `in` increments on acceptance and `out` only on delivery to a reader. Strict overflow refuses without a counter; ring overflow evicts and increments destination `dropped`. Assert exact counts; accepting a wrong-kind/second destination, accepting a step past the counter limit or storing any part of it, counting a delivery that is not a forwarding step, checking B against S or A's Owner, bypassing either hop's ACL or a destination rule, clearing the field on route revocation, keeping a source copy, changing the sender, omitting `original_to` or changing a counter on refusal fails. Apply [PubSub routing](../../docs/constitution.md#pubsub-routing): publish directly and by forwarding with all-success, all-failed and mixed outcomes, a forwarding subscriber included. All-failed answers the caller with an error; mixed keeps every accepted copy, answers success and warns in both the daemon log and syslog. A failing branch must not prevent another delivery; test each recipient order and assert exact delivery counts, and verify no warning carries a body or credential. Verify the router counters with three accepted copies (`in` +1, `out` +3), two accepted and one failed (+1, +2), total failure (+0, +0) and no recipients (+0, +0); consuming an accepted copy increments `out` no second time. Counting a failed copy or a publication attempt, or losing the forward counter across fan-out, fails its check |
| K.16 | Carry the model through API, CLI, MCP, web and launchers | K.4–K.15 | Exercise creation, editing, transfer, Groups, status and private values through supported faces. Forms retain daemon authority; names and replies remain stable through launcher lifecycle. New state labels replace banned/disabled controls. Every line-list editor enforces the `#` marker on save, reporting the offending line rather than guessing a kind or silently retyping it. The `ab-*` launchers register their session agents as Personal, and such a record stays off the shared pages while its Owner still reaches it. Faces distinguish a configured route from one the destination's current ACL keeps usable, show the forwarding record as the identity checked there rather than the original sender, and never claim that a usable route admits every sender. Real browsers verify ordinary, delegated and refused paths; removing face wiring fails an end-to-end check |
| K.17 | Exercise installed reinstall and recovery | K.1–K.16 except K.1.2 | Run K.1.4 on a fresh host and over a stopped 0.6 installation. Populate through 0.7 APIs, then verify accepted IDs, ownership, credentials, configuration, queues and durable counters across SQLite backup/restore, graceful restart and bus-child crash. Verify failed-update recovery with matching 0.7 state and binaries. Run affected SSH, account-isolation, browser-role/session and live-runtime gates; fixtures alone do not close installed acceptance |
| K.18 | Reconcile docs and publish release evidence | K.17 | Topic docs, glossary, CLI examples and decision history agree with the constitution and tested behavior. Mark replaced rules as superseded; move completed work/evidence to DONE and `done/`. Set the shared release version and changelog when shipping; verify package contents and clean-install instructions. No unresolved required row is reported complete The line starts at 0.7.0 with the first code change and takes ordinary PATCH bumps with `CHANGELOG.0.7.md`; once every row passes, the commit `STABLE - passed all tests and reviews` marks it and the next line is 0.8 |

## Verification

Apply the [0.7 architecture requirements](../../docs/10-modules.md#07-implementation-requirements)
throughout K.1–K.16. K.1 records a reproducible performance baseline; K.17 compares
lookup, authorization, management writes and concurrent send/consume at increasing
registry and backlog sizes on SQLite. Record latency, allocations, write
volume and lock contention. Check that an isolated record edit, including
ownership transfer, does not rewrite unrelated records or queued messages.
Agent/record removal is the explicit exception: its transaction removes every
stored reference to the removed name before publishing the complete view, so
later name reuse inherits nothing. Ownership transfer publishes a complete view
without a storage reload; no reload may appear on lookup, authorization or
delivery paths. Registry/authority reads do not query storage.
Investigate regressions and record justified tradeoffs before release.

K.3/K.5 implement [batched statistics persistence](../../docs/10-modules.md#statistics-persistence).
Use a controlled clock to count writes during traffic bursts and idle periods:
flushes follow the defined cadence, increments cause no individual writes and
empty flushes write nothing. Verify concurrent increments, retry without duplicate
counts, graceful shutdown and last-use timestamps. Include PubSub router counters
in successful-checkpoint restart, graceful restart and failed-flush retry checks;
verify their documented crash-loss boundary without creating a PubSub queue. Data changes commit immediately
without waiting for the statistics flush.

SQLite owns queue contents and their durable counters while retaining the
accepted one-minute/graceful-shutdown checkpoint and crash-loss boundary. The
0.6 JSON dump and credentials are not imported by the clean reinstall. Live readers and browser
sessions remain process-local. Storage checks must exercise real SQLite.
The [additional backends](../R1/storage.md#backends) are R1 work.

**Review cadence.** Every ten commits on the 0.7 line, counted from the 0.7.0
commit, a Fable reviewer reads the code and docs changed since the previous
review; one more full review precedes the STABLE commit. Findings are
reproduced before they are accepted, and fixed or recorded as pending work.

Each behavioral change requires `src/smoke.sh --slow`, including vet and race
checks. Every credited mutation must compile and fail a relevant assertion;
test rejection, concurrency and persistence through the actual changed paths.
Installed gates remain separate from unit/handler tests. Planning-only edits
require link, scope, dependency and question-migration checks; no runtime version
bump or test-suite rerun is required for this file.
