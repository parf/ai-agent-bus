# TODO 0.7.0 — constitution

📌 **TL;DR:** Implement the constitution across storage, authority, lifecycle and every face.

## Objective

Deliver the [constitution](../../docs/constitution.md#project-constitution) in
the shared 0.7 release line. The constitution owns the model; this file owns
unfinished work and acceptance. All implementation rows are pending; K.1.2 has moved to R1. Existing behavior counts
as a starting point, not proof that the new contract is satisfied.

Keep the constitution's wording and structure. Update the numbered contracts
and user guides as each change lands. Existing [MVP acceptance](TODO.md#remaining-work)
remains open under its original IDs; completing this plan does not by itself
complete MVP.

## Starting point

Source review at 0.6.16: records and credentials use name references; Groups are
membership lists; records carry `disabled`; Service secrets accept nonempty
bytes. Administrative writes checkpoint before success but may mutate memory
before persistence. Five kinds, nested membership, Personal restrictions,
compacted JSON configuration and PubSub Deliver-To already have implementations.

## Next step

Start K.1, then SQLite and exclusive access in K.1.1/K.1.3, followed by K.2–K.6
and then the executable cutover in K.1.4. Settle the remaining
[forwarding details](QUESTIONS.md#constitution-forwarding) before K.15;
unrelated work can proceed. A reload API/SIGHUP remains optional
and is not a required 0.7 feature.

## Storage and identity

An exhaustive deletion-specific crash matrix is excluded from 0.7. Agent and
record removal still use the commit-then-publish invariant and the focused
stale-grant checks in K.3/K.5. Users are never removed.

| ID | Work | Depends on | Acceptance |
|---|---|---|---|
| K.1 | Inspect existing state and define the [0.6 → 0.7 cutover](0.7-cutover.md#inventory) | — | Read-only inventory covers owners, identities, tokens, groups, statuses, private values and backlog without exposing secrets. Specify backup, restore validation, treatment of incompatible records and recovery to matching binaries/state. A fixture containing a self-owned Agent record, a Service credential and an incompatible record must name all three; removing any detection fails. Keep the existing pre-1.1 compatibility policy; do not silently discard or reinterpret data |
| K.1.1 | Implement the default [SQLite store](../../docs/constitution.md#persistence-and-loading) behind persistence ports | K.1 | Select and package a supported driver; persist durable entities, credentials, queue contents and durable queue counters. Management changes are transactional; queue state retains its periodic checkpoint boundary without a write per message. Validate database version and integrity on load; unreadable, incompatible or orphaned queue state cannot silently become an empty node. Protect database and backup files with the existing account boundary. Removing the transaction or accepting an orphaned queue fails a named check |
| K.1.2 | Moved to [R1 storage](../R1/TODO.md#storage-acceptance) | — | Not a 0.7 prerequisite |
| K.1.3 | Enforce one active daemon per SQLite database | K.1.1 | Competing starts against the same database cannot both serve. After exclusivity is lost, every read and write is refused with a stated reason and none is answered from cached state; this acceptance does not require the process to exit or stay running. Verify normal release, crash recovery and restart without overlapping active instances. Serving any cached request after loss fails a process-level check |
| K.2 | Persist stable entity IDs, timestamps and derived indexes | K.1.3 | Entity IDs/lifecycle fields and daemon configuration survive restart; runtime facts are not stored as configuration. User IDs are never freed; record IDs are never reused after record removal, and exhaustion cannot wrap into an existing ID. Load a complete validated view and rebuild indexes. Internal IDs stay out of public identities and routing. Reusing a record ID or omitting an index rebuild fails a named check |
| K.3 | Apply management changes through write-through persistence | K.2 | Registration, profiles, Groups, management, credentials and private-value writes validate and commit in the selected backend before memory publication. Core publishes one complete replacement view from the validated committed change rather than mutating live maps in place or reloading storage; a publication failure refuses every read/write with a reason. Failed validation or pre-commit save exposes no partial update; test late storage failures and restart outcomes explicitly. Publishing before commit, retaining old authority after commit or allowing a stale queue batch to rewrite policy fails its check |
| K.4 | Enforce User ownership everywhere | K.2, K.3 | Records, Groups and daemon ownership resolve to Users. Agent-created resources still have a User owner. Reject Agent owners on creation, transfer and restore; cover enrolment, launcher startup, self-owned records and orphan cleanup. A transfer is one committed record update; `@owner` resolves from the atomically published complete view without unrelated writes or a storage reload. Suspension follows the direct User owner; no Agent ownership-chain exception remains. Allowing any non-User owner fails the corresponding path's check |
| K.5 | Bind credentials to typed User/Agent IDs and invalidate stale grants | K.2–K.4 | Reject Queue/PubSub/Service credentials on issue and restore. Removing an Agent or record transactionally deletes its old tokens and every ACL, Maintainer, Group-member and Deliver-To reference. Recreate an Agent name under another User and verify none returns before or after restart. Preserve rotation, suspension and account/session checks; persist last-use timestamps within the write budget. Skipping each credential or reference class separately fails its check |
| K.6 | Complete User profile validation and lifecycle fields | K.2, K.3 | Check unique normalized email, GitHub and case-insensitive Twitter/X names on add, edit, import and restore; person names remain nonunique. Preserve enrolment keys and profile/photo data. Invalid multi-field edits are atomic. Bypassing any uniqueness check fails its negative case |
| K.1.4 | Implement the [offline 0.6 → 0.7 state rewrite](0.7-cutover.md#rewrite-and-activation) | K.1, K.1.1, K.1.3, K.2–K.6 | With the daemon stopped, read the inventory and write a separately validated SQLite candidate without mutating the source. The tool emits a mapping template containing every non-User-owned object with its stable identity and a blank Owner; activation requires the completed file to match that inventory object-for-object and every Owner to be an existing active User. Existing Queue/PubSub/Service credentials require explicit removal entries and appear by principal and fingerprint, never secret. Nonconforming secrets must be fixed before activation. Preserve every class named in the cutover runbook. Reports and mappings use the daemon account boundary; external readability fails. Omitting any mapping/removal or dropping any preserved class fails its check |

## Authority and lifecycle

| ID | Work | Depends on | Acceptance |
|---|---|---|---|
| K.7 | Authorize existing state; validate the whole candidate | K.3–K.5 | Old Owner can transfer; new Owner governs immediately and after restart. A submitted grant cannot authorize itself. Maintainers cannot alter protected fields; Agent self-management and Owner-only Personal changes hold. Concurrent revocation cannot race a stale authorization into a write. Each bypass has a failing check |
| K.8 | Add typed actor syntax while retaining existing forms | K.5, K.7 | Plain names, `@group`, `*` and `@owner` retain their rules. `#agent` validates kind and resolves to the unchanged canonical name. Trim whitespace and handle duplicates consistently; one invalid term rejects all. Personal restrictions hold. Removing kind validation or leaking `#` into routing fails a check |
| K.9 | Give Groups User Owners and assigned Maintainers | K.4, K.7, K.8 | Assigned Users/Agents manage ordinary Group membership; only permitted authority changes Maintainers. Protected `@administrators` has no Group Owner or Maintainers, and only the daemon Owner changes its direct membership. Nested cycles terminate; revocation takes effect. Supplying either forbidden field or a Group Maintainer attempting to grant daemon administration fails acceptance |
| K.10 | Add atomic list add/remove operations | K.3, K.8, K.9 | ACL, Maintainers, Group members and Deliver-To support concurrent deltas without lost updates. PubSub Deliver-To validates typed actors. On User/Agent/Queue it accepts at most one destination channel: add succeeds only when empty; an occupied add returns an error naming the field, remove clears it, and replacement is an explicit whole-field write. A whole-field write with two destinations or a wrong-kind term is refused and stores nothing. An invalid mixed delta changes nothing; concurrent adds cannot silently overwrite or no-op |
| K.11 | Replace User states and record Disabled with active/inactive | K.4, K.5, K.7 | No banned state or second delivery switch remains. User inactivity suspends access and directly owned records without deleting tokens/backlog; record inactivity blocks reads/writes, releases affected waits and accounts for dropped PubSub copies. An active Administrator may reactivate an ordinary User but cannot change another Administrator; the daemon Owner may reactivate either, and must remain active. Removing the peer-Administrator refusal or bypassing authority, socket/session or pending-reader checks fails acceptance |
| K.12 | Hide inactive Services while reserving names | K.7, K.11 | Inactive Services appear in no discovery, lookup or secret read; registration at the same name fails. Authorized status edits can reactivate the existing record without recreation. Cover every face and restored state. Removing hiding or uniqueness enforcement fails separate checks |
| K.13 | Validate env secrets and Agent JSON configuration | K.3, K.7 | Basic env syntax is enforced without application-specific validation; JSON is validated and compacted before hashing. Enforce kind and read/write authority, retain digest-only public responses and reject invalid updates atomically. A nonconforming secret is rejected on write and target-state validation; no legacy exception is loaded. Invalid env input, wrong-principal config reads and uncompact hashing each have a failing check |
| K.14 | Log direct entity edits to daemon log files | K.3 | A direct edit to a User, registry record, or Group writes one log-file entry with authenticated User-or-Agent actor, operation, target, result, and the real client IP when available. Unix requests invent no IP. Credential operations, lifecycle-state changes, reads, sends, and consumes write none. Exercise both actor kinds, edits authenticated by a canary token and carrying canary secret/configuration values, plus separate canary traffic. Omitting an entity class or required field, misidentifying the actor, losing/inventing an IP, logging an excluded operation, or disclosing any sensitive value fails |

## Delivery and release

| ID | Work | Depends on | Acceptance |
|---|---|---|---|
| K.15 | Forward User, Agent and Queue traffic | K.8–K.11; [forwarding decisions](QUESTIONS.md#constitution-forwarding) | Implement the zero-or-one destination-channel meaning of `deliver_to` on User, Agent and Queue while retaining its typed-actor list meaning on PubSub; a second destination is an error, never replacement, no-op or truncation. Forward at most once: when a destination would forward again, refuse the original send with a stated error before storage and leave both inboxes unchanged. Refuse configuration unless Q87's route principal may write to the destination, and refuse delivery while that route is no longer usable without clearing it. The route grants nothing. Apply the destination's current access to the original sender, then its active state, TTL/deadline, bound, overflow and counter rules exactly as for a direct send. Any refusal answers the original sender before storage or counters change; a later route grant resumes forwarding without an edit. Move exactly one message to the destination with the original sender and required single-valued `original_to`: source `in`/`out` stay unchanged, destination `in` increments on acceptance and `out` only on delivery to a reader. Strict overflow refuses without a counter; ring overflow evicts and increments destination `dropped`. Assert exact counts; accepting a wrong-kind/second destination, accepting or storing any part of a second-hop send, widening the sender's access through the route, bypassing a destination rule, clearing the field on route revocation, keeping a source copy, changing the sender, omitting `original_to` or changing a counter on refusal fails |
| K.16 | Carry the model through API, CLI, MCP, web and launchers | K.4–K.15 | Exercise creation, editing, transfer, Groups, status and private values through supported faces. Forms retain daemon authority; names and replies remain stable through launcher lifecycle. New state labels replace banned/disabled controls. Faces distinguish a configured route from one whose route principal currently keeps it usable, and never claim that a usable route admits every sender. Real browsers verify ordinary, delegated and refused paths; removing face wiring fails an end-to-end check |
| K.17 | Exercise the installed cutover and recovery | K.1–K.16 except K.1.2 | Fresh install and populated cutover through K.1.4 preserve accepted IDs, mapped ownership, compatible credentials, configuration, queue contents and durable counters on SQLite. The report names every removed incompatible credential. Verify one SQLite backup/restore unit, graceful restart, bus-child crash and failed upgrade recovery with matching state/binaries. Run affected SSH, account-isolation, browser-role/session and live-runtime gates; record actual limitations rather than treating fixtures as live acceptance |
| K.18 | Reconcile docs and publish release evidence | K.17 | Topic docs, glossary, CLI examples and decision history agree with the constitution and tested behavior. Mark replaced rules as superseded; move completed work/evidence to DONE and `done/`. Set the shared release version and changelog when shipping; verify package contents and clean-install instructions. No unresolved required row is reported complete |

## Verification

Apply the [0.7 architecture requirements](../../docs/10-modules.md#07-implementation-requirements)
throughout K.1–K.16. K.1 records a reproducible performance baseline; K.17 compares
lookup, authorization, management writes and concurrent send/consume at increasing
registry and backlog sizes on SQLite. Record latency, allocations, write
volume and lock contention. Check that an isolated record edit, including
ownership transfer, does not rewrite unrelated records or queued messages.
Agent/record removal is the explicit exception: its transaction removes every
stored reference to the removed name before publishing the complete view, so
later name reuse inherits nothing. Ownership transfer publishes a complete view
without a storage reload; no reload may appear on lookup, authorization or
delivery paths. Registry/authority reads do not query storage.
Investigate regressions and record justified tradeoffs before release.

K.3/K.5 implement [batched statistics persistence](../../docs/10-modules.md#statistics-persistence).
Use a controlled clock to count writes during traffic bursts and idle periods:
flushes follow the defined cadence, increments cause no individual writes and
empty flushes write nothing. Verify concurrent increments, retry without duplicate
counts, graceful shutdown and last-use timestamps. Data changes commit immediately
without waiting for the statistics flush.

SQLite owns queue contents and their durable counters while retaining the
accepted one-minute/graceful-shutdown checkpoint and crash-loss boundary. The
JSON dump is cutover input, not a second active store. Live readers and browser
sessions remain process-local. Storage checks must exercise real SQLite.
The [additional backends](../R1/storage.md#backends) are R1 work.

Each behavioral change requires `src/smoke.sh --slow`, including vet and race
checks. Every credited mutation must compile and fail a relevant assertion;
test rejection, concurrency and persistence through the actual changed paths.
Installed gates remain separate from unit/handler tests. Planning-only edits
require link, scope, dependency and question-migration checks; no runtime version
bump or test-suite rerun is required for this file.
