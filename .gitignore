/src/agent-bus
/src/agent-busd
tmp/
/src/mcp/node_modules/
/src/*.test
/src/**/*.test

# A TLS private key is never ours to publish, and localhost.direct's is
# revoked the moment one appears in a repo (docs/05-discovery.md#dashboard).
*.key
*.crt
*.pem
