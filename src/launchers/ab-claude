#!/bin/sh
set -eu
command -v bun >/dev/null 2>&1 || { echo 'ab-claude: install bun to run the launcher' >&2; exit 1; }
dir=$(dirname "$(readlink -f "$0")")
entry="$dir/launcher.js"
[ -f "$entry" ] || entry="$dir/launcher.ts"
exec bun "$entry" claude "$@"
