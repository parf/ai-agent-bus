---
description: Who and what is on the agent bus
---

Call `ab_ls` and show the result as a short table: name, kind, description.
If an argument was given, treat it as a `kind` filter and pass it through.

Argument: $ARGUMENTS
