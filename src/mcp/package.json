{
  "name": "@agent-bus/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "agent-bus MCP face: four tools over the daemon's HTTP+JSON",
  "scripts": {
    "start": "bun run server.ts",
    "smoke": "bun run smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.9.0"
  }
}
